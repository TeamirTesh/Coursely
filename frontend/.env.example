# Backend API proxy target (used by Vite dev server only)
# In production, configure your host's reverse proxy (Vercel rewrites, Nginx, etc.)
# to route /api/* to your deployed backend URL instead.
VITE_API_TARGET=http://localhost:8000
